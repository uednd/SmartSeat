# SmartSeat Terminal Firmware

This directory is an ESP32-P4 firmware project skeleton for the SmartSeat terminal.

It is initialized only. Display behavior, MQTT communication, status lights, and mmWave presence sensor integration are not implemented.

Future firmware work should use ESP-IDF and keep hardware-specific presence sensor code behind a dedicated adapter layer.
